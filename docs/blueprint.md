# Links MAP：プロジェクト設計指針（Blueprint）

## 1. プロジェクト概要
本作は、ユーザーが複数の「作品世界（World）」を管理し、その中の実体と関係性をグラフ構造で可視化・編集する創作支援エディタである。
「点（Entity）」と「線（Connection）」の組み合わせにより、複雑な物語の相関図を動的に構築する。

## 2. コア思想：Everything is a Connection
情報の重複を排除し、かつ柔軟な拡張性を持たせるため、以下の設計思想を徹底する。

- **Entities（実体）の極小化**: 
  Entityテーブルは名前と識別子のみを持つ。その他の属性はここには持たせない。
- **Connections（関係性）への集約**: 
  あらゆる属性、エピソード、キャラクター同士の因縁は、すべてConnectionテーブルに集約する。
- **自己参照プロフィール (The Heart of the System)**:
  - `from_id` と `to_id` が同一のConnectionを、その実体自身の「詳細・プロフィール」として定義する。
  - 「自分自身との関係」が「自分自身の説明」となる抽象度の高いデータモデルを実現する。

## 3. 実装ガイドライン（Frontend / UI）

### A. ルーティングと防御（Routing Guard）
`react-router-dom` の Layout 構文（Outlet）を活用し、認証状態に応じた堅牢なアクセス制御を行う。

- **PublicRoute (公開ルート)**: 
  - `LandingPage` 等、非ログイン時のみアクセス可能なルート。
  - ログイン済みのユーザーがアクセスした場合は、自動的に `/dashboard` へリダイレクトする。
- **ProtectedRoute (保護ルート)**: 
  - ログイン必須のルート。旧 `AuthLayout`。
  - 未ログイン時は `/` へリダイレクトする。
  - **共通UIの集約**: サイドバー（ツールバー）やトップバー（ユーザー情報）はこのLayoutに実装し、子ルート（Dashboard, Settings等）は `Outlet` を通じて描画する。

### B. フロントエンドアーキテクチャ
- **AuthContext**: `supabase.auth.onAuthStateChange` を監視し、アプリ全体にユーザー情報とロード状態を提供する。
- **Component化の徹底**: ページレベルのコンポーネントは、責務が分割された小さなUIコンポーネントを組み合わせる。
- **React Flow**: `connection_tags.graph_type` に応じて振る舞いを出し分ける。
  - `'self'`: 詳細パネル（Markdownエディタ）として表示。
  - `'edge'`: 二点間の接続線。
  - `'box'`: `parentNode`（包含関係）として視覚化。

### C. スタイリング（MUI / Material-UI）
- **テーマによる一元管理（厳守）**: 
  - すべての色、タイポグラフィ、スペーシングは `src/theme/theme.ts` で定義する。
  - **ハードコードの禁止**: コンポーネント内の `sx` prop や `style` に直接カラーコードやピクセル値を記述してはならない。必ず `theme.palette` や `theme.spacing` を経由する。
  - **背景と透明度**: 背景色（`background.default`）やグラデーションを活かすため、Layoutや各コンポーネントの背景は原則 `transparent` または `action.hover` 等のテーマ定義済みの色を使用し、白い塗りつぶしを避ける。

## 4. セキュリティと共有
- **マルチテナント**: すべてのデータは `world_id` に紐づき、所有者（owner_id）のみがアクセスできる。
- **RLS (Row Level Security)**: SupabaseのRLSを活用し、データベースレベルで「自分のデータ以外は操作不可」を担保する。
- **QR共有機能**: `access_tickets` による期間限定・権限限定のゲスト編集機能を予定。

## 5. 開発ロードマップ

### フェーズ1：認証と基本UIの構築（完了直前）
1. **認証基盤の実装**:
   - `AuthProvider` および `PublicRoute` / `ProtectedRoute` によるガードの実装（完了）。
   - パスワードリセット機能を含む `useAuth` フックの整備（完了）。
2. **ダッシュボードのプロトタイプ**:
   - DaVinci Resolveのようなダークで高級感のあるレイアウト構築。
   - サイドバー、トップバー、および「ワールド新規作成」ダイアログの実装。
3. **DB連動（現在地）**:
   - `docs/schema.sql` に基づく `worlds` テーブルの構築。
   - ワールドの新規保存・一覧取得ロジックの実装。

### フェーズ2：コア機能の実装
1. Entity作成時の「プロフィール用自己参照Connection」自動生成。
2. React Flowを用いたグラフエディタのプロトタイプ作成。
3. 包含関係（'box'）の描画サポート。

### フェーズ3：高度な機能
1. `access_tickets` を用いたゲスト編集機能。
2. タイムライン機能（'flow' 接続）。

## 6. データベーススキーマ
`docs/schema.sql` を正典とする。変更が必要な場合は必ずこのファイルを更新し、ドキュメントとの整合性を保つこと。